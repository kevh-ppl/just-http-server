#include <stdio.h>
#include <sys/socket.h>

int main(int argc, char const *argv[])
{
    //first i need a file descriptor for the server
    int server_fd;
    //then i need another one for the client
    int client_conn;

    
}