# My first http server in C

This is my first attempt to code a http server.

Sorry, no Makefile right now.

**Use:**
```
gcc -Wall -Wextra -pedantic -o server -Iinclude src/parser.c src/server.c src/utils.c src/main.c
```

Dios, ayúdame.
